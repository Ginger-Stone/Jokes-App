import React from 'react'
import styles from '../../styles/components/defaults/loader.module.scss'

function Loader() {
    return (
        <div className={styles.loader}>
            Loading ...
        </div>
    )
}

export default Loader
